# Authentication setup

The site supports email registration and login. Every account must choose a unique username (3–30 letters, numbers, or underscores). Accounts are stored in `api/data/accounts.sqlite`, and the browser receives an HttpOnly PHP session cookie.
